// HaxBall advances its native timer from physics disc 0.
export const NATIVE_CLOCK_BALL_DISC_ID = 0;

// Game logic resolves this named disc as the visible football in managed modes.
export const GAME_BALL_DISC_REF = "gameBall";

// Any non-zero motion advances the native timer; the clock disc has damping 1.
export const NATIVE_CLOCK_BALL_START_SPEED = 0.01;
